# 人工智能课程学习（课内ppt+gpt）

## PPT 07 深度学习（一）神经网络
### 1. 概述
- 从传统机器学习到神经网络，深度学习希望把“人工设计特征”变成“模型自动学习特征”
- exp：图像分类
  - 低层：边缘和方向
  - 中层：纹理和部件
  - 高层：物体语义
- 深度学习的核心优势之一是可以自动学习特征表示，减少人工特征工程的负担。
- 输入层：接收原始数据。
- 隐藏层：对输入进行加工、提取特征。
- 输出层：给出最终预测结果。
### 2. 神经网络
   
  2.1.1. 假设空间
  - 在线性回归里，假设空间是所有线性函数，比如 $y = wx + b$。
  - 在线性分类里，假设空间是所有线性分类边界。
  - 而在神经网络里，假设空间变成了很多层函数的复合。
  - 一种复杂的可调函数：权重+偏置（训练的过程即调整这些）

  2.1.2. 损失函数
  - 平方损失和**交叉熵损失**（配合softmax输出类别概率）

  2.1.3. 优化算法
  - 梯度下降：沿着梯度的反方向更新参数，让损失变小。
  - 反向传播、batch、epoch、learning rate

  2.2.一层神经网络
  - 一个神经元：先做一个线性组合，再过一个激活函数（非线性）。
  - **非线性激活函数**，网络才有能力表达复杂关系。
  - 线性叠线性，还是线性函数
  - exp：$XOR问题$ 没法用一条直线把两类点分开，神经网络通过**隐藏层**和**非线性激活函数**，可以把原始空间变换到新的表示空间，让原来线性不可分的问题变得可分。

  2.3. 权重初始化
  - 不能将权重全部初始化为0，不同神经元的工作没有区分 -> **随机初始化**打破对称性

  2.4. 训练超参数：Epoch、Batch Size、Iteration
  - （一个）epoch（整个训练集被完整使用了一遍） = （all） iteration（模型参数被更新了一次）-> batch size（一次iteration处理多少图片）
  - 每取一个 batch，模型做一次前向传播，算一次损失，做一次反向传播，并更新一次参数。
  2.5 训练过程
  1. 取一个batch
  2. 前向传播
  - 将每个样本从输入层送进网络，$n+1$ 层接受 $n$ 层输出，得到预测结果
  3. 计算损失
  - 和真实标签比较 -> 得到一个具体数字评价准确性（一般用一个batch的平均值）
  4. 反向传播
  - 如果某个参数增大会让损失变大，那我们就应该把它调小；如果某个参数增大会让损失变小，那我们就可以把它调大。
  - 计算损失函数对每个参数的**梯度**，用**链式法则**一层一层往回传
    - 梯度：方向（决定参数应增大还是减小）+ 大小（决定每个参数对结果多敏感、应该改多少）
    - 链式法则：反向传播 = 上游梯度 × 局部梯度
  5. 参数更新
  - $w \leftarrow w - \eta \frac{\partial L}{\partial w}$
  - $w$ 是某个参数，$L$ 是损失函数，$\frac{\partial L}{\partial w}$ 是损失对这个参数的梯度，$\eta$ 是学习率（步长）。

### 3. 训练稳定性
- 张量（Tensor）：用来存放数据的多维数组。
  - 神经网络里的输入、权重、激活值、梯度，基本都用张量来表示。
- 网络层数增加，梯度连乘 -> 结果可能变化夸张
  - **梯度消失**：梯度总$<1$。
    - 浅层参数几乎不更新，训练非常慢
    - 经常和 sigmoid（有饱和区，输入特别大/小时，输出接近1/0；导数上界较小）、tanh 有关
    - 偏爱 $ReLU$ ：在正半轴导数为1，更有利于缓解梯度消失。形式大概是 $f(x)=\max(0,x)$。当 $x>0$ 时，它的导数是 1。
  - **梯度爆炸**：参数更新就会特别剧烈，loss 剧烈震荡，甚至直接变成 NaN
    - 网络太深、权重初始化太大、学习率太高（互相加重）
- 训练稳定性方法：
  1. 适当的激活函数（ReLU）
  2. 适当的权重初始化：不能太大或太小，影响梯度
  3. 归一化：将输入值限制在激活函数敏感（梯度较大）的区域内
  4. Dropout：防止过拟合的方法。训练时随机“关掉”一部分神经元，让网络不能过分依赖某几个节点。
  5. ResNet（残差网络）：改变神经网络框架结构（乘->加）。用“跳跃连接”或者“恒等映射”，让某一层的输入 $x$ 可以绕过中间若干层，直接加到后面的输出上，形式类似 $F(x)+x$。

### 4. 自动微分与可视化
- 自动微分(AD)：深层复杂的神经网络不易直接写出梯度解析式，构建计算图进行自动微分。
  - 现代深度学习框架，比如 PyTorch、TensorFlow，会自动帮你记录计算过程，然后根据计算图自动求梯度(记录运算依赖关系的图)。
- 可视化：
  - 神经网络内部很复杂，像黑箱一样。可视化技术试图通过梯度、特征图、激活值等方式，把模型“关注了什么”“学到了什么”展示出来。
  - 更直观地理解模型，而不只是看最终准确率。

### . CNN（卷积神经网络）（PPT8前导）
- **普通全连接神经网络**：上一层的每个神经元都和下一层的每个神经元相连。
  - 也能处理图像，（拉平成一个向量，然后输入到全连接网络里。每个像素变成一个输入维度）但图片变大，问题就非常明显。
      1. 参数量太大，导致训练慢，内存压力巨大、过拟合风险极高。
      2. 破坏了图像的空间结构，“上下左右相邻”这种二维空间关系被弱化了
      3. 没有利用图像特征的平移共享性，全连接网络做不到天然共享，它会给不同位置的连接分配不同参数，因此效率很低。（同一局部在不同位置效果不一样）

## PPT 08 深度学习（二）卷积神经网络$CNN$
### 1. 卷积神经网络结构上的三大特性：局部连接、权重共享、下采样
  1. 局部连接:全连接层里，一个神经元要看整张图；卷积层里，一个神经元只看图像的一小块区域