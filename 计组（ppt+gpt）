# 计组（ppt+gpt）

## PPT06 指令流水线

### 一、 流水线为什么出现，以及五阶段流水线的基本模型

- CPU 同时处理多条指令的不同阶段？
  - 流水线方式下单条指令执行时间不能缩短，但能大大提高指令吞吐率。
- eg. Load指令的5个阶段
  - 取指 2ns，寄存器读 1ns，ALU 2ns，存储器读 2ns，寄存器写 1ns。
    - 单周期处理器的时钟周期必须按最慢的 lw 来定，也就是 8ns
    - 流水线处理器的周期由最长阶段决定，这里最长阶段是 2ns，所以五级流水线中 N 条指令大约需要 (4+N)×2ns。
      - 当 N 很大时，性能接近提高 4 倍；如果五个阶段完全均衡，就可以接近 5 倍。
  - 只有当指令数很多时，中间稳定区才接近每周期完成一条指令，也就是理想 CPI 接近 1。
- 适合流水线的指令集：（MIPS）
  - 指令长度尽量一致、
  - 格式少、
  - 寄存器字段位置固定、
  - 只有 load/store 能访问存储器、
  - 内存数据尽量对齐
- 不同类型指令塞进流水线需要处理
  - R-type 指令只需四阶段，但第四阶段就写回有**结构冒险**（资源冲突）
  - 解决：把 R-type 的写回延后到第五阶段，中间加一个空的 MEM 阶段，也就是 NOP，让所有指令统一成五个阶段。
- 处理器对比：
  - 单周期处理器的问题是时钟周期被最慢指令拖长；
  - 多周期处理器让一条指令分阶段复用部件；
  - 流水线处理器则让多条指令重叠执行。
- 五阶段流水线： IF → ID → EX → MEM → WB

### 二、五阶段流水线数据通路与控制信号

- 整体结构
  - 每个阶段之间必须加**寄存器**，把这一周期阶段末尾产生的结果保存下来，交给下一周期继续用。
  - `IF/ID `寄存器：取指阶段的输出
    - 典型包括当前取到的指令和 PC+4。
  - `ID/EX` 寄存器：译码/读寄存器阶段的输出
    - 比如寄存器堆读出的 busA、busB，符号扩展或零扩展后的立即数，rs、rt、rd 字段，以及后续阶段要用的控制信号。
  - `EX/MEM` 寄存器：执行阶段的输出
    - 比如 ALU 运算结果、是否相等的 Zero、分支目标地址、store 要写入内存的数据、目的寄存器编号，以及 MEM/WB 阶段还要继续使用的控制信号。
  - `MEM/WB` 寄存器：访存阶段的输出
    - 比如数据存储器读出的数据、ALU 结果、目的寄存器编号，以及最后写回阶段需要的控制信号
- 控制信号
  - 控制信号在 ID 阶段产生，但要按 EX、MEM、WB 分组随指令向后传递。
  - EX 阶段控制信号包括 ExtOp、ALUSrc、ALUOp、RegDst。
    - ExtOp 决定立即数符号扩展还是零扩展；
    - ALUSrc 决定 ALU 的 B 输入来自寄存器 busB 还是立即数；
    - ALUOp 辅助局部 ALU 控制逻辑决定具体 ALU 操作；
    - RegDst 决定目的寄存器选 rd 还是 rt。
  - MEM 阶段控制信号包括 MemWr 和 Branch，
    - 前者控制是否写数据存储器，后者说明当前是不是分支指令。
  - WB 阶段控制信号包括 MemtoReg 和 RegWr，
    - 前者决定写回数据来自内存还是 ALU，后者决定是否写寄存器。
- 同一时刻看竖列，是多条指令分别处在不同阶段；同一条指令看横行，是它按周期依次经过 IF、ID、EX、MEM、WB。

### 三、 流水线冒险

1. 结构冒险 Structural Hazard
- 硬件资源冲突：同一个部件在同一个周期被多条指令同时需要。
  - 某条 lw 指令正在 MEM 阶段读数据，同时后面的指令又在 IF 阶段取指令，这两个动作都要访问存储器
- 解决：增加/分离硬件资源，或者让某些指令停一下
  - 流水线数据通路通常把 Instruction Memory 和 Data Memory 分开，也就是取指令用 IM，访问数据用 DM
- 原则是：每个部件在特定阶段被使用
2. 数据冒险 Data Hazard
- 核心是：后面的指令要用前面指令的结果，但前面指令还没有把结果写回寄存器
- 转发 Forwarding / Bypassing：既然结果已经在流水线内部出现，就可以直接把它送给后面的 ALU 输入
- 常见判断方式：看寄存器编号是否相同
ALU 指令之后紧跟使用结果，通常可以靠转发解决，不一定需要停顿。 例如：

```
add $1, $2, $3
sub $4, $1, $5
```

add 的 ALU 结果在它 EX 阶段结束时已经产生，而 sub 真正需要 $1 是在它自己的 EX 阶段。两者时间上能接上，所以用 EX/MEM 到 ALU 输入的转发即可。
- load-use hazard: lw 后面紧跟使用结果 
  - ALU-use 可转发，load-use 要停一拍(stall)
    - 随后的指令需要被阻塞一个时钟(bubble空操作)
- 第一，能转发就转发，主要解决 ALU 结果相关。
- 第二，转发也来不及时，就阻塞一拍，典型是 load-use。（stall）
- 第三，编译器也可以调整指令顺序，在 load 和使用它的指令之间提前插入一条无关指令，减少硬件 stall 的性能损失。
3. 控制冒险 Control Hazard / Branch Hazard
- 下一条指令地址还没确定，但流水线已经继续取指了
- 转移或异常改变执行流程，后继指令在目标地址产生前已被取出。
- 处理方法：
  - 阻塞：遇到分支，就等分支结果确定后再取下一条。性能差
  - 分支预测：静态（永远预测不跳转、、）/动态预测（根据程序运行历史来猜）/缩短
    - 基本逻辑：先猜一个方向取指，猜对继续；猜错fiush（清掉）错误进入流水线的指令，重新取指
- 分支延迟槽（MIPS思想）
  - 编译器优化指令顺序，编译器尽量把一条安全、有用的指令放进这个位置or `NOP`


```
forwarding 转发；
stall 是暂停等待；
bubble 是插入空操作；
flush 是清除错误路径指令
```

### 四、 高级流水线技术

- 高性能流水线——指令级并行 ILP 技术
    - 在一段普通程序的指令序列里，寻找可以同时或重叠执行的指令
  - 超流水线 Superpipelining：把某些阶段继续切得更细
  - 多发射流水线 Multiple Issue：一个周期可以发射多条指令（很难持续操作）
    - 静态多发射VLIW（超长指令字）
      - 静态多发射由编译器在编译时进行相关性分析和静态分支预测，静态完成“指令打包”和“冒险处理”；同一个周期内发射的多条指令被看成一条有多个操作的长指令，称为一个发射包。
      - 与机器结构密切相关，运行时灵活性较差
    - 动态多发射（超标量 Superscalar）
      - 处理器硬件在运行时自己判断哪些指令可以同时发射
  - 动态调度：在等待解决阻塞时，到后面找指令提前执行
    - 按序发射、乱序执行、按序提交
      - 前端按程序顺序取指、译码；
      - 中间有多个功能单元和保留站，可以乱序执行；
      - 后端有提交单元，也就是 Reorder Buffer，保证最终结果按原程序顺序提交
    - “推测”：由编译器或处理器猜测指令执行结果，并据此调整指令执行顺序，使指令执行达到尽可能大的并行；推测可能错误，所以需要错误检测和回滚机制，并区分软件推测和硬件推测
      - 软件推测主要由编译器完成，硬件推测由处理器运行时完成